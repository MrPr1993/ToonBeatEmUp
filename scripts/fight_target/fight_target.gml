/// @description fight_target(obj)
/// @param obj
function fight_target(argument0) {
	isPlayer=id selfatk.isPlayer=isPlayer
	var i=-1;

	i= instance_nth_nearest(x,y,argument0,id)

	targetEnemy=i

	reFocusTime=4



}
